import 'package:flutter/material.dart';

class AppRoutes {
  static const String startingView = '/';

  static Map<String, WidgetBuilder> routes = {
    // startingView: (context) => const StartingView(),
  };
}
